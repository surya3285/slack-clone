# Demo credentials only - replace before using anywhere beyond local learning.
apiVersion: v1
kind: Secret
metadata:
  name: mongo-credentials
  namespace: slack-clone
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: root
  MONGO_INITDB_ROOT_PASSWORD: changeme

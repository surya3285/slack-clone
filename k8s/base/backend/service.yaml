apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: slack-clone
  labels:
    app: backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
